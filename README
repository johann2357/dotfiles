Here you can find my dotfiles.

- vim
  * My setup is:
    * neovim with iTerm
    * macvim as GUI (more than 256 colors)
  * Caveat:
    * Plugin `command-t` might be tricky to install (but it's worth it)

- fonts
  * Powerline (for vim usage)
